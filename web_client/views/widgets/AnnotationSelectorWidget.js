import View from 'girder/views/View';
import events from '../../events';
var AnnotationSelector = View.extend({
    events: {},
    initialize(){
      
    },
    render(){
      return this;
    }
});

export default AnnotationSelector;